{% extends 'overlay.html.twig' %}

{% block body %}
    <div {{ stimulus_controller('overlay', { mercureUrl: mercure('https://simply-stream.com/tracker/{id}/total')}) }}>
        <div class="total-number" {{ stimulus_target('overlay', 'total') }}>{{ total }}</div>
    </div>
{% endblock %}
